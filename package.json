{
  "name": "@proofcomputer/switchboard-express-demo",
  "version": "0.1.8",
  "description": "Rich Express demo app for Switchboard launch-demo.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/proof-computer/switchboard-express-demo.git"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "preview": "npm run build && node scripts/preview.mjs",
    "visual": "npm run build && node scripts/visual.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "pack:dry-run": "npm pack --dry-run --json"
  },
  "dependencies": {
    "@proofcomputer/switchboard-express": "github:proof-computer/switchboard-express#v0.1.1",
    "@proofcomputer/switchboard-sdk": "github:proof-computer/switchboard-sdk#v0.1.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@proofcomputer/switchboard-express"
    ]
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  }
}
